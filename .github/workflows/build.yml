name: Build

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.2.app

      - name: Build
        run: >
          xcodebuild -project searchy.xcodeproj
          -scheme searchy
          -configuration Release
          CODE_SIGN_IDENTITY=""
          CODE_SIGNING_REQUIRED=NO
          CODE_SIGNING_ALLOWED=NO
          build

      - name: Lint Python
        run: |
          pip install ruff
          ruff check searchy/*.py

      - name: Test Python
        run: |
          pip install pytest
          python3 -m pytest searchy/tests/ -x -q
